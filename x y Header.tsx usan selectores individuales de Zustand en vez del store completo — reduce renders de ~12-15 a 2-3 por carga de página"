[1mdiff --git a/App.tsx b/App.tsx[m
[1mindex 09ac9c1..11eddf8 100644[m
[1m--- a/App.tsx[m
[1m+++ b/App.tsx[m
[36m@@ -115,21 +115,36 @@[m [mconst MainLayout = () => {[m
 };[m
 [m
 function App() {[m
[31m-    const { initializeAuth, isAuthenticated, isProfileLoading } = useAppStore();[m
[31m-    [m
[31m-    // FIX CRÍTICO: Solo inicializar UNA VEZ cuando la app arranca[m
[32m+[m[32m    // FIX CRÍTICO (re-renders x12): antes se hacía `useAppStore()` sin selector,[m
[32m+[m[32m    // lo que suscribía a App.tsx al store COMBINADO completo (auth + clients +[m
[32m+[m[32m    // projects + finance + team + notifications + jobs + portal + inbox).[m
[32m+[m[32m    // Cualquier set() en cualquier slice —incluidos los ~14 fetch en cascada[m
[32m+[m[32m    // que dispara initializeAuth() al arrancar (fetchClients, fetchJobs,[m
[32m+[m[32m    // fetchProjects, fetchFinanceData, etc.)— forzaba un re-render de App,[m
[32m+[m[32m    // aunque App solo lee 3 campos del slice de auth.[m
[32m+[m[32m    //[m
[32m+[m[32m    // Usar selectores individuales hace que Zustand solo notifique a este[m
[32m+[m[32m    // componente cuando ESE valor concreto cambia (comparación por referencia[m
[32m+[m[32m    // en cada campo primitivo), en vez de ante cualquier cambio del store.[m
[32m+[m[32m    const initializeAuth = useAppStore(state => state.initializeAuth);[m
[32m+[m[32m    const isAuthenticated = useAppStore(state => state.isAuthenticated);[m
[32m+[m[32m    const isProfileLoading = useAppStore(state => state.isProfileLoading);[m
[32m+[m
[32m+[m[32m    // initializeAuth es estable (definida una sola vez al crear el slice),[m
[32m+[m[32m    // así que este efecto se ejecuta una única vez por montaje de App.[m
     useEffect(() => {[m
[31m-        console.log("🎬 App.tsx: Inicializando autenticación...");[m
[32m+[m[32m        if (import.meta.env.DEV) console.log("🎬 App.tsx: Inicializando autenticación...");[m
         initializeAuth();[m
     }, [initializeAuth]);[m
 [m
     // Mostrar spinner mientras se verifica si hay sesión[m
     if (isProfileLoading) {[m
[31m-        console.log("⏳ App.tsx: Cargando perfil...");[m
         return <LoadingFallback />;[m
     }[m
 [m
[31m-    console.log("✅ App.tsx: Renderizando rutas. isAuthenticated =", isAuthenticated);[m
[32m+[m[32m    if (import.meta.env.DEV) {[m
[32m+[m[32m        console.log("✅ App.tsx: Renderizando rutas. isAuthenticated =", isAuthenticated);[m
[32m+[m[32m    }[m
 [m
     return ([m
             <>[m
[1mdiff --git a/components/layout/Header.tsx b/components/layout/Header.tsx[m
[1mindex 734e285..81eeb66 100644[m
[1m--- a/components/layout/Header.tsx[m
[1m+++ b/components/layout/Header.tsx[m
[36m@@ -24,7 +24,16 @@[m [mconst NotificationIcon = ({ link }: { link: string }) => {[m
 };[m
 [m
 const Header: React.FC<HeaderProps> = ({ onMenuClick }) => {[m
[31m-  const { profile, logout, notifications, markAllAsRead, markAsRead } = useAppStore();[m
[32m+[m[32m  // FIX (mismo patrón que App.tsx): antes `useAppStore()` sin selector suscribía[m
[32m+[m[32m  // a Header al store COMBINADO completo, re-renderizando en cascada con cada[m
[32m+[m[32m  // fetch en segundo plano (clients, projects, jobs...) aunque Header solo[m
[32m+[m[32m  // necesite profile/notifications/logout. Selectores individuales limitan[m
[32m+[m[32m  // el re-render a cuando ESE campo concreto cambia.[m
[32m+[m[32m  const profile = useAppStore(state => state.profile);[m
[32m+[m[32m  const notifications = useAppStore(state => state.notifications);[m
[32m+[m[32m  const logout = useAppStore(state => state.logout);[m
[32m+[m[32m  const markAllAsRead = useAppStore(state => state.markAllAsRead);[m
[32m+[m[32m  const markAsRead = useAppStore(state => state.markAsRead);[m
   const navigate = useNavigate(); // Hook para redirección[m
 [m
   const [isNotificationsOpen, setIsNotificationsOpen] = useState(false);[m
